# 要求をタスクへ分解し実装計画を作る

- レクチャーID: `sec-02-lec-03`
- セクション: 2. AIDD開発の準備と進め方
- 種別: `exercise`
- 目安時間: 6分
- ステータス: `planned`

## セクションの目的

Codexを利用しながらも、講師が設計と検証の責任を持つ開発サイクルを作る

## レクチャーの目的

講座の完成形を小さな実装タスクと検証タスクへ分解する

## Codexへの指示文

以下をそのままCodexに渡し、出力された差分と検証結果を確認してから採用します。

```text
あなたはこのリポジトリのシニア開発者です。Codexを使ったAIDDの一環として、次のレクチャーだけを実施してください。

レクチャーID: sec-02-lec-03
目的: 講座の完成形を小さな実装タスクと検証タスクへ分解する
セクションの前提: Codexを利用しながらも、講師が設計と検証の責任を持つ開発サイクルを作る

依頼:
- ユーザーストーリーと受け入れ条件を整理する
- ローカル、AWS、コンソール確認の順序を決める

対象範囲（実際の分割はレビューで調整可）:
- docs/development/backlog.md
- curriculum.yaml

方針:
- 受け入れ条件を先に確認し、実装と検証を一つずつ進めてください。結果が分かる証拠を残してください。
- 既存の実装・規約・カリキュラムを先に読み、無関係なリファクタリングをしない。
- AWSの実リソースを変更する場合は、適用前にTerraform planとコスト影響を確認する。
- シークレット、アクセストークン、個人情報、実アカウント固有値をコードや資料へ書き込まない。
- 変更後は差分を要約し、実行したコマンドと結果、未解決リスクを報告する。

完了条件:
- 実装バックログとセクション別チェックポイント

検証:
- 各タスクに実装内容と完了条件が設定されている

この依頼の範囲を越える変更は行わず、判断が必要な点は実装前に質問または仮定として明示してください。
```

## 進行

- ユーザーストーリーと受け入れ条件を整理する
- ローカル、AWS、コンソール確認の順序を決める

## 生成される目安の成果物

実際のファイル分割やパスは、Codexの提案とレビューで変わって構いません。収録時点では次を目安にします。

- `docs/development/backlog.md` — 講義・運用資料の目安
- `curriculum.yaml` — 機械可読な定義の目安

## 動作確認

- 各タスクに実装内容と完了条件が設定されている

## 収録メモ

- まずCodexへの指示文を提示し、生成された差分・テスト・実行結果を確認する。
- ナレーションはElevenLabsで場面単位に生成し、AWS・Spring・Terraformの用語発音を確認する。
- スクリーンキャストは講師が実際の操作と検証結果を収録する。

## コミット

このレクチャー終了時のコミットメッセージは、レクチャー名と同じ **要求をタスクへ分解し実装計画を作る** とする。
