# 本番ビルド成果物とリリースチェック

- レクチャーID: `sec-06-lec-04`
- セクション: 6. ローカル統合と品質確認
- 種別: `review`
- 目安時間: 5分
- ステータス: `planned`

## セクションの目的

AWSへ進む前に、ローカル環境でアプリの主要フローを再現可能にする

## レクチャーの目的

AWSへ配布する成果物と、デプロイ前の品質ゲートを確定する

## Codexへの指示文

以下をそのままCodexに渡し、出力された差分と検証結果を確認してから採用します。

```text
あなたはこのリポジトリのシニア開発者です。Codexを使ったAIDDの一環として、次のレクチャーだけを実施してください。

レクチャーID: sec-06-lec-04
目的: AWSへ配布する成果物と、デプロイ前の品質ゲートを確定する
セクションの前提: AWSへ進む前に、ローカル環境でアプリの主要フローを再現可能にする

依頼:
- JAR、React成果物、設定値の扱いを確認する
- テスト、秘密情報、ログ出力のチェックを行う

対象範囲（実際の分割はレビューで調整可）:
- Makefile
- scripts/build-backend.sh
- scripts/build-frontend.sh
- scripts/package-application.sh

方針:
- コードを変更する前に、設計・テスト・セキュリティ・運用の観点でレビューしてください。指摘と採否理由を記録してください。
- 既存の実装・規約・カリキュラムを先に読み、無関係なリファクタリングをしない。
- AWSの実リソースを変更する場合は、適用前にTerraform planとコスト影響を確認する。
- シークレット、アクセストークン、個人情報、実アカウント固有値をコードや資料へ書き込まない。
- 変更後は差分を要約し、実行したコマンドと結果、未解決リスクを報告する。

完了条件:
- デプロイ前チェックリスト

検証:
- 同じコミットからデプロイ成果物を再生成できる

この依頼の範囲を越える変更は行わず、判断が必要な点は実装前に質問または仮定として明示してください。
```

## 進行

- JAR、React成果物、設定値の扱いを確認する
- テスト、秘密情報、ログ出力のチェックを行う

## 生成される目安の成果物

実際のファイル分割やパスは、Codexの提案とレビューで変わって構いません。収録時点では次を目安にします。

- `Makefile` — 実行・検証用ファイルの目安
- `scripts/build-backend.sh` — 実行・検証用ファイルの目安
- `scripts/build-frontend.sh` — 実行・検証用ファイルの目安
- `scripts/package-application.sh` — 実行・検証用ファイルの目安

## 動作確認

- 同じコミットからデプロイ成果物を再生成できる

## 収録メモ

- まずCodexへの指示文を提示し、生成された差分・テスト・実行結果を確認する。
- ナレーションはElevenLabsで場面単位に生成し、AWS・Spring・Terraformの用語発音を確認する。
- スクリーンキャストは講師が実際の操作と検証結果を収録する。

## コミット

このレクチャー終了時のコミットメッセージは、レクチャー名と同じ **本番ビルド成果物とリリースチェック** とする。
