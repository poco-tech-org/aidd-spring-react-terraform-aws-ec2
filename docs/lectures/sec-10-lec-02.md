# Terraform plan差分とコンソールの照合

- レクチャーID: `sec-10-lec-02`
- セクション: 10. 総合動作確認と講座の仕上げ
- 種別: `review`
- 目安時間: 4分
- ステータス: `planned`

## セクションの目的

実装、インフラ、コンソール確認、教材を提出可能な品質へ揃える

## レクチャーの目的

コード、state、実リソースの差分を確認し、意図しない変更を残さない

## Codexへの指示文

以下をそのままCodexに渡し、出力された差分と検証結果を確認してから採用します。

```text
あなたはこのリポジトリのシニア開発者です。Codexを使ったAIDDの一環として、次のレクチャーだけを実施してください。

レクチャーID: sec-10-lec-02
目的: コード、state、実リソースの差分を確認し、意図しない変更を残さない
セクションの前提: 実装、インフラ、コンソール確認、教材を提出可能な品質へ揃える

依頼:
- 最終terraform planをレビューする
- 重要リソースのコンソール表示と照合する

対象範囲（実際の分割はレビューで調整可）:
- scripts/check-terraform-drift.sh
- docs/verification/terraform-console.md

方針:
- コードを変更する前に、設計・テスト・セキュリティ・運用の観点でレビューしてください。指摘と採否理由を記録してください。
- 既存の実装・規約・カリキュラムを先に読み、無関係なリファクタリングをしない。
- AWSの実リソースを変更する場合は、適用前にTerraform planとコスト影響を確認する。
- シークレット、アクセストークン、個人情報、実アカウント固有値をコードや資料へ書き込まない。
- 変更後は差分を要約し、実行したコマンドと結果、未解決リスクを報告する。

完了条件:
- 最終インフラ照合チェックリスト

検証:
- 意図しない差分がなく、確認結果を記録できる

この依頼の範囲を越える変更は行わず、判断が必要な点は実装前に質問または仮定として明示してください。
```

## 進行

- 最終terraform planをレビューする
- 重要リソースのコンソール表示と照合する

## 生成される目安の成果物

実際のファイル分割やパスは、Codexの提案とレビューで変わって構いません。収録時点では次を目安にします。

- `scripts/check-terraform-drift.sh` — 実行・検証用ファイルの目安
- `docs/verification/terraform-console.md` — 講義・運用資料の目安

## 動作確認

- 意図しない差分がなく、確認結果を記録できる

## 収録メモ

- まずCodexへの指示文を提示し、生成された差分・テスト・実行結果を確認する。
- ナレーションはElevenLabsで場面単位に生成し、AWS・Spring・Terraformの用語発音を確認する。
- スクリーンキャストは講師が実際の操作と検証結果を収録する。

## コミット

このレクチャー終了時のコミットメッセージは、レクチャー名と同じ **Terraform plan差分とコンソールの照合** とする。
