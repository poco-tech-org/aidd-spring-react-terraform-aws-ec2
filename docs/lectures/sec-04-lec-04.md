# Reactのビルドとバックエンド連携

- レクチャーID: `sec-04-lec-04`
- セクション: 4. Reactフロントエンドを作る
- 種別: `demo`
- 目安時間: 5分
- ステータス: `planned`

## セクションの目的

認証とAPI連携を後から追加できる、操作可能なReact画面を作る

## レクチャーの目的

Reactの本番ビルド成果物をデプロイ構成へ渡せる状態にする

## Codexへの指示文

以下をそのままCodexに渡し、出力された差分と検証結果を確認してから採用します。

```text
あなたはこのリポジトリのシニア開発者です。Codexを使ったAIDDの一環として、次のレクチャーだけを実施してください。

レクチャーID: sec-04-lec-04
目的: Reactの本番ビルド成果物をデプロイ構成へ渡せる状態にする
セクションの前提: 認証とAPI連携を後から追加できる、操作可能なReact画面を作る

依頼:
- 本番用環境変数とビルドコマンドを確認する
- Spring Bootとの配信方法とAPIパスを整理する

対象範囲（実際の分割はレビューで調整可）:
- frontend/vite.config.js
- frontend/src/App.jsx
- scripts/package-application.sh

方針:
- 目的と完了条件を確認し、最小の変更で成果物を作成してください。
- 既存の実装・規約・カリキュラムを先に読み、無関係なリファクタリングをしない。
- AWSの実リソースを変更する場合は、適用前にTerraform planとコスト影響を確認する。
- シークレット、アクセストークン、個人情報、実アカウント固有値をコードや資料へ書き込まない。
- 変更後は差分を要約し、実行したコマンドと結果、未解決リスクを報告する。

完了条件:
- 再現可能なフロントエンドビルド手順

検証:
- 本番ビルドが成功し、想定したAPIへ接続できる

この依頼の範囲を越える変更は行わず、判断が必要な点は実装前に質問または仮定として明示してください。
```

## 進行

- 本番用環境変数とビルドコマンドを確認する
- Spring Bootとの配信方法とAPIパスを整理する

## 生成される目安の成果物

実際のファイル分割やパスは、Codexの提案とレビューで変わって構いません。収録時点では次を目安にします。

- `frontend/vite.config.js` — React／Node成果物の目安
- `frontend/src/App.jsx` — React／Node成果物の目安
- `scripts/package-application.sh` — 実行・検証用ファイルの目安

## 動作確認

- 本番ビルドが成功し、想定したAPIへ接続できる

## 収録メモ

- まずCodexへの指示文を提示し、生成された差分・テスト・実行結果を確認する。
- ナレーションはElevenLabsで場面単位に生成し、AWS・Spring・Terraformの用語発音を確認する。
- スクリーンキャストは講師が実際の操作と検証結果を収録する。

## コミット

このレクチャー終了時のコミットメッセージは、レクチャー名と同じ **Reactのビルドとバックエンド連携** とする。
