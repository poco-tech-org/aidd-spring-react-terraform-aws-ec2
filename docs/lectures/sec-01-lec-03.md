# 開発環境・AWS料金・削除方針

- レクチャーID: `sec-01-lec-03`
- セクション: 1. 講座の全体像と完成形
- 種別: `setup`
- 目安時間: 5分
- ステータス: `planned`

## セクションの目的

完成するアプリ、構成、学習の進め方を把握し、最後までの到達点を明確にする

## レクチャーの目的

講座を進めるためのローカル環境とAWS利用時のコスト管理方針を整える

## Codexへの指示文

以下をそのままCodexに渡し、出力された差分と検証結果を確認してから採用します。

```text
あなたはこのリポジトリのシニア開発者です。Codexを使ったAIDDの一環として、次のレクチャーだけを実施してください。

レクチャーID: sec-01-lec-03
目的: 講座を進めるためのローカル環境とAWS利用時のコスト管理方針を整える
セクションの前提: 完成するアプリ、構成、学習の進め方を把握し、最後までの到達点を明確にする

依頼:
- Java、Node.js、Terraform、AWS CLI、Gitを確認する
- 検証後にterraform destroyを実行する運用を決める

対象範囲（実際の分割はレビューで調整可）:
- README.md
- infra/terraform.tfvars.example
- docs/submission/checklist.md

方針:
- 既存の規約と構成を確認したうえで、再現可能な設定・手順を追加してください。秘密情報は例示値のみにしてください。
- 既存の実装・規約・カリキュラムを先に読み、無関係なリファクタリングをしない。
- AWSの実リソースを変更する場合は、適用前にTerraform planとコスト影響を確認する。
- シークレット、アクセストークン、個人情報、実アカウント固有値をコードや資料へ書き込まない。
- 変更後は差分を要約し、実行したコマンドと結果、未解決リスクを報告する。

完了条件:
- 環境確認結果とAWSリソース削除チェックリスト

検証:
- 必要なCLIのバージョン確認とAWS認証確認が完了する

この依頼の範囲を越える変更は行わず、判断が必要な点は実装前に質問または仮定として明示してください。
```

## 進行

- Java、Node.js、Terraform、AWS CLI、Gitを確認する
- 検証後にterraform destroyを実行する運用を決める

## 生成される目安の成果物

実際のファイル分割やパスは、Codexの提案とレビューで変わって構いません。収録時点では次を目安にします。

- `README.md` — 講義・運用資料の目安
- `infra/terraform.tfvars.example` — 関連成果物の目安
- `docs/submission/checklist.md` — 講義・運用資料の目安

## 動作確認

- 必要なCLIのバージョン確認とAWS認証確認が完了する

## 収録メモ

- まずCodexへの指示文を提示し、生成された差分・テスト・実行結果を確認する。
- ナレーションはElevenLabsで場面単位に生成し、AWS・Spring・Terraformの用語発音を確認する。
- スクリーンキャストは講師が実際の操作と検証結果を収録する。

## コミット

このレクチャー終了時のコミットメッセージは、レクチャー名と同じ **開発環境・AWS料金・削除方針** とする。
